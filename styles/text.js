import { StyleSheet } from 'react-native';

import { infoColor } from './variables';

export default StyleSheet.create({
  connectedServer: {
    color: infoColor,
    marginTop: 70,
    textAlign: 'center',
  },
});
